<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--
        チェックボックス（UI 設計書 6.2）。担当: 波1-D。
        17×17（サブタスクは 15×15）、角丸3、チェック済みはアクセント地に白いチェック、ホバーで枠を濃く。
        未チェックの枠は Stroke.Control ではなく Stroke.ControlStrong を使う。
        Stroke.Control（#D6D6D6）は白地で 1.5:1 しかなく、操作できる境界として見えないため。
    -->
    <Style x:Key="TaskCheckBoxStyle" TargetType="CheckBox">
        <Setter Property="Width" Value="{StaticResource CheckBoxSize}" />
        <Setter Property="Height" Value="{StaticResource CheckBoxSize}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource AppFocusVisualStyle}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Border x:Name="Box"
                            Width="{TemplateBinding Width}"
                            Height="{TemplateBinding Height}"
                            Background="Transparent"
                            BorderBrush="{DynamicResource StrokeControlStrongBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource RadiusCheckBox}"
                            SnapsToDevicePixels="True">
                        <Path x:Name="Mark"
                              Data="{StaticResource IconCheck}"
                              Stretch="Uniform"
                              Margin="2.5"
                              Stroke="{DynamicResource TextOnAccentBrush}"
                              StrokeThickness="2"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              StrokeLineJoin="Round"
                              Visibility="Collapsed" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource TextSecondaryBrush}" />
                            <Setter TargetName="Box" Property="Background" Value="{DynamicResource SurfaceHoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Box" Property="Background" Value="{DynamicResource AccentDefaultBrush}" />
                            <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource AccentDefaultBrush}" />
                            <Setter TargetName="Mark" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="True" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Box" Property="Background" Value="{DynamicResource AccentHoverBrush}" />
                            <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource AccentHoverBrush}" />
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SubtaskCheckBoxStyle" TargetType="CheckBox" BasedOn="{StaticResource TaskCheckBoxStyle}">
        <Setter Property="Width" Value="{StaticResource SubtaskCheckBoxSize}" />
        <Setter Property="Height" Value="{StaticResource SubtaskCheckBoxSize}" />
    </Style>

    <!-- 文字つきのチェック（設定画面など）。四角は 17px のまま、右にラベルを置く -->
    <Style x:Key="LabeledCheckBoxStyle" TargetType="CheckBox">
        <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}" />
        <Setter Property="FontSize" Value="{StaticResource TypeBodySmallSize}" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="MinHeight" Value="{StaticResource ButtonHeight}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource AppFocusVisualStyle}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" Background="Transparent">
                        <Border x:Name="Box"
                                Width="{StaticResource CheckBoxSize}"
                                Height="{StaticResource CheckBoxSize}"
                                VerticalAlignment="Center"
                                Background="Transparent"
                                BorderBrush="{DynamicResource StrokeControlStrongBrush}"
                                BorderThickness="1"
                                CornerRadius="{StaticResource RadiusCheckBox}"
                                SnapsToDevicePixels="True">
                            <Path x:Name="Mark"
                                  Data="{StaticResource IconCheck}"
                                  Stretch="Uniform"
                                  Margin="2.5"
                                  Stroke="{DynamicResource TextOnAccentBrush}"
                                  StrokeThickness="2"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  StrokeLineJoin="Round"
                                  Visibility="Collapsed" />
                        </Border>
                        <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource TextSecondaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Box" Property="Background" Value="{DynamicResource AccentDefaultBrush}" />
                            <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource AccentDefaultBrush}" />
                            <Setter TargetName="Mark" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
