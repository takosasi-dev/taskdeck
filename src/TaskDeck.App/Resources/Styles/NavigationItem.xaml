<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--
        サイドバーの項目（UI 設計書 6.5）。担当: 波1-C。
        選択中は地を1段濃くし、左端に 3×18 のアクセント縦バー。アイコンは選択時だけアクセント色。
        地の色はモックアップ（ライト #E6E6E6／ダーク #2D2D2D）に合わせ、既存トークンで最も近い
        Stroke.Divider を使っている（Surface.Hover はライトのサイドバー地 #F3F3F3 の上でほぼ見えないため）。
    -->

    <!-- キーボード操作のときだけ出るフォーカス枠（UI 設計書 8章） -->
    <Style x:Key="FocusVisualRowStyle">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle Margin="0"
                               RadiusX="5" RadiusY="5"
                               Stroke="{DynamicResource AccentDefaultBrush}"
                               StrokeThickness="2"
                               SnapsToDevicePixels="True" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ListBoxItem 側は入れ物に徹する（選択の見た目は項目が自分で描く） -->
    <Style x:Key="NavItemContainerStyle" TargetType="ListBoxItem">
        <Setter Property="Padding" Value="0" />
        <!-- 右の 6px はスクロールバーの通り道（SidebarView 参照） -->
        <Setter Property="Margin" Value="0,0,6,0" />
        <Setter Property="AutomationProperties.Name" Value="{Binding Label}" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <ContentPresenter />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="NavItemButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisualRowStyle}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{StaticResource RadiusRow}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource StrokeSubtleBrush}" />
                        </Trigger>
                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource StrokeDividerBrush}" />
                        </DataTrigger>
                        <!-- タスクをドラッグして重ねている（落とすとそのプロジェクトへ・そのタグを付ける UI 設計書 15.5） -->
                        <DataTrigger Binding="{Binding IsDropTarget}" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource SurfaceSelectedBrush}" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 選択中の左端バー -->
    <Style x:Key="NavSelectionBarStyle" TargetType="Border">
        <Setter Property="Width" Value="3" />
        <Setter Property="Height" Value="18" />
        <Setter Property="CornerRadius" Value="2" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Background" Value="{DynamicResource AccentDefaultBrush}" />
        <Setter Property="Visibility" Value="Collapsed" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                <Setter Property="Visibility" Value="Visible" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- アイコン: 選択時だけアクセント色 -->
    <Style x:Key="NavIconStyle" TargetType="Path" BasedOn="{StaticResource IconPathStyle}">
        <Setter Property="Stroke" Value="{DynamicResource TextPrimaryBrush}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                <Setter Property="Stroke" Value="{DynamicResource AccentDefaultBrush}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="NavLabelStyle" TargetType="TextBlock" BasedOn="{StaticResource TextBodyStyle}">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                <Setter Property="FontWeight" Value="SemiBold" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- 件数（右端の等幅数字） -->
    <Style x:Key="NavCountStyle" TargetType="TextBlock" BasedOn="{StaticResource TabularCaptionStyle}">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="MinWidth" Value="18" />
        <Setter Property="TextAlignment" Value="Right" />
    </Style>

    <Style x:Key="NavSectionLabelStyle" TargetType="TextBlock" BasedOn="{StaticResource TextLabelStyle}">
        <Setter Property="Margin" Value="14,6,10,4" />
    </Style>

    <Style x:Key="NavSeparatorStyle" TargetType="Border">
        <Setter Property="Height" Value="1" />
        <Setter Property="Margin" Value="10,8,10,6" />
        <Setter Property="Background" Value="{DynamicResource StrokeDividerBrush}" />
    </Style>

    <!-- プロジェクトの色の点 -->
    <Style x:Key="ProjectDotStyle" TargetType="Ellipse">
        <Setter Property="Width" Value="9" />
        <Setter Property="Height" Value="9" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
</ResourceDictionary>
